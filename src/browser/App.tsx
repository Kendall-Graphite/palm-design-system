// Rolodex browser — placeholder until Phase 2 shell is built
export default function App() {
  return (
    <div style={{ fontFamily: 'var(--font-sans)', padding: 'var(--space-8)', color: 'var(--color-ink-900)' }}>
      <h1 style={{ fontSize: 'var(--text-4xl)', fontWeight: 'var(--weight-semibold)', letterSpacing: 'var(--tracking-tight)', lineHeight: 'var(--leading-tight)' }}>
        Palm
      </h1>
      <p style={{ marginTop: 'var(--space-2)', fontSize: 'var(--text-base)', color: 'var(--color-ink-700)' }}>
        Design system component browser — Phase 2 coming next.
      </p>
    </div>
  );
}
